# 第二十章复习：Map/Set 集合响应式

> 状态：待作答

## 1. 为什么 Map/Set 不能直接复用普通对象的 get/set Proxy 处理器？

请说明原生集合方法对 `this` 和内部槽位的要求，以及集合代理需要怎样包装方法。

### 你的答案

<!-- 在这里填写你的答案。 -->

## 2. Map 的具体键依赖、size 依赖和迭代依赖分别解决什么问题？

请用 `get()`、`size` 和 `keys()` 或 `entries()` 各举一个例子。

### 你的答案

<!-- 在这里填写你的答案。 -->

## 3. 为什么 `map.set(已有 key, 新 value)` 通常不应该触发 size 依赖？

请比较新增 key 和更新已有 key 时，集合结构发生了什么变化。

### 你的答案

<!-- 在这里填写你的答案。 -->

## 4. `Map.keys()` 和 `Map.values()` 的依赖边界为什么不同？

请分析已有 key 的 value 改变时，两种遍历是否都应该重新执行，并说明原因。

### 你的答案

<!-- 在这里填写你的答案。 -->

## 5. raw key 与 reactive key 为什么需要统一身份？

请描述同一个原对象先后作为 raw key 和 reactive key 传给 `Map.get()` 时，如果不做统一会出现什么错误。

### 你的答案

<!-- 在这里填写你的答案。 -->

## 6. `delete()` 不存在的 key 或 `clear()` 空集合时，为什么不应该触发依赖？

请从“实际状态是否发生变化”和“避免无效 effect 执行”两个角度回答。

### 你的答案

<!-- 在这里填写你的答案。 -->
